<br>
<h5>Listing Grades</h5>
<br>

<table>
  <thead>
    <tr>
      <th>Student</th>
      <th>Exam</th>
      <th>Score</th>
      <th>Feedback</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @grades.each do |grade| %>
      <tr>
        <td><%= grade.student.full_name %></td>
        <td><%= grade.exam.title %></td>
        <td><%= best_in_place grade, :score, :path => [@course, @exam, grade], :inner_class => "best_input" %></td>
        <td><%= best_in_place grade, :feedback, :path => [@course, @exam, grade], :as => :input, :nil => "Add feedback" %></td>
        <td><%= link_to 'Show', [@course, @exam, grade] %></td>
        <td><%= link_to 'Edit', [:edit, @course, @exam, grade ] %></td>
        <td><%= link_to 'Destroy', [@course, @exam, grade], method: :delete, data: { confirm: 'Are you sure?' } %></td>
        
      </tr>
    <% end %>
  </tbody>
</table>
  
  <br>
  <div class="panel callout radius">
    <p>List of all students who should graded if I only knew how to work that out </p>
    <p>Group: <strong><%= @course.group.group_name %></strong></p>
    <p>Course: <strong><%= @course.course_name %></strong></p>
    
    <% @course.group.students.each do |student|  %>
      <p>
        <strong> > <%= link_to student.full_name, student %></strong>
      </p>
    <% end %>
  </div>
  
  
  <br>
<%= link_to 'New Grade', new_course_exam_grade_path %>

