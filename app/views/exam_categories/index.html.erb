<% provide(:title, 'Exam Categories') %>

<div class="page-title">
  <h1>Exam Categories</h1>
</div>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th width="20%" colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @exam_categories.each do |exam_category| %>
      <tr>
        <td><%= link_to exam_category.title, exam_category %></td>
        <td><%= link_to 'Edit', edit_exam_category_path(exam_category) %></td>
        <td><%= link_to 'Destroy', exam_category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Exam category', new_exam_category_path, class: 'button tiny radius' %>
